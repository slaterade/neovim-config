require("toggleterm").setup {
  autochdir = true,       -- auto change shell directory to vim current directory
  direction = 'float',    -- floating terminal in middle of screen
  start_in_insert = true, -- start in insert mode
}

